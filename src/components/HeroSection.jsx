import React from 'react';
import './HeroSection.css';

const HeroSection = () => {
    return (
        <section className="hero">
            <h2>Find Eco-Friendly Businesses Near You</h2>
            <p>Join us in making the world a greener place. Sign up to be the first to know when we launch!</p>
        </section>
    );
};

export default HeroSection;
